```sh
# Установка зависимостей
pip install -r requirements.txt

# Подготовка файла с адресами
# Заполните файл 'addresses.txt' публичными адресами, каждый на новой строке

# Запуск скрипта
python main.py
